# x, y = 20, 40
# print((lambda: y, lambda: x)[x > y]())

# x, y = 20, 40
# print({True: x, False: y} [x > y]) 


# Exercise 1 : Hello World
# Instructions
# Print the following output in one line of code:
# Hello world
# Hello world
# Hello world
# Hello world

print("\nHello world"*5)

# Exercise 2 : Some Math
# Instructions
# Write code that calculates the result of: (99^3) * 8 (99 to the power of 3 times 8)

print((99**3)*8)


# Exercise 3 : What Is The Output ?
# Instructions
# Predict the output of the following code snippets:
# >>> 5 < 3
# >>> 3 == 3
# >>> 3 == "3"
# >>> "3" > 3
# >>> "Hello" == "hello"
False
True
False
"not a valid operator for type string and number"
False



# 🌟 Exercise 4 : Your Computer Brand
# Instructions
# Create a variable called computer_brand which value is the brand name of your computer.
# Using the computer_brand variable print a sentence that states the following: "I have a <computer_brand> computer".
computer_brand = "HP EliteBook 845 G17"
print(f"I have a {computer_brand} computer")



# 🌟 Exercise 5 : Your Information
# Instructions
# Create a variable called name, and set it’s value to your name.
# Create a variable called age, and set it’s value to your age.
# Create a variable called shoe_size, and set it’s value to your shoe size.
# Create a variable called info and set it’s value to an interesting sentence about yourself. The sentence must contain all the variables created in parts 1, 2 and 3.
# Have your code print the info message.
# Run your code

name,age,shoeSize = "Jason",25,7
info = (f"{name} is {age} and shoe size is {shoeSize}")
print (info)



# 🌟 Exercise 6 : A & B
# Instructions
# Create two variables, a and b.
# Each variable value should be a number.
# If a is bigger then b, have your code print Hello World.

a,b = 40,12
print({True: "Hello World", False: "dlorW olleH"} [a > b]) 


# Exercise 7 : Odd Or Even
# Instructions
# Write code that asks the user for a number and determines whether this number is odd or even.
number = int(input("Input a number"))
print({True: f"{number} is even", False: f"{number} is odd"} [number % 2 ==0]) 



# 🌟 Exercise 8 : What’s Your Name ?
# Instructions
# Write code that asks the user for their name and determines whether or not you have the same name, print out a funny message based on the outcome.
storeName = "maman"
userName = str(input("Input your name").lower())
print({True: f"You has same name{storeName} as the CEO", False: f"Hello Welcome {userName}"} [storeName == userName]) 



# 🌟 Exercise 9 : Tall Enough To Ride A Roller Coaster
# Instructions
# Write code that will ask the user for their height in inches.
# If they are over 145cm print a message that states they are tall enough to ride.
# If they are not tall enough print a message that says they need to grow some more to ride.

height = float(input("Input height in inches:"))
convertHeight = height * 2.54
print({True: "You are tall enough to ride", False: "Grow some more to ride"} [convertHeight > 154]) 






